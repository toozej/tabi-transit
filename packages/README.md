# Shared TypeScript packages

`transit-domain` contains framework-independent domain types and pure helpers.
`api-client` is reserved for code generated from the executable OpenAPI contract
by WP-02; generated code is never hand edited.
